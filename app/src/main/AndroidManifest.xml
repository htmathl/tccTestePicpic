<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testepicpic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.tela_cadastro5"></activity>
        <activity android:name=".activity.tela_cadastro2" />
        <activity android:name=".activity.tela_cadastro1"></activity>
        <activity android:name=".activity.tela_cadastro3"></activity>
        <activity android:name=".activity.tela_cadastro4"></activity>

        <activity
            android:name=".activity.overview"
            android:label="@string/title_activity_overview" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>